#!/bin/bash

# Command line tools
sudo apt install btop nvtop tmux tree -y

# Editing gnome and extensions
sudo apt install dconf-editor gnome-tweaks gnome-shell-extension-manager -y

# Utilities
sudo apt install solaar spotify-client -y

# Productivity tools
sudo apt install google-chrome-stable -y
flatpak install flathub net.cozic.joplin_desktop -y

# Programming tools
sudo apt install code -y
sudo apt install git-lfs -y

